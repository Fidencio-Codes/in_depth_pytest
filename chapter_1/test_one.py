# Chapter 1 

def test_passing():
    assert (1,2,3) == (1,2,3)


# Running unspecified pytest in terminal causes pytest to search files of current directories and sub-directories 

# Ran 'pytest --tb=no' 
    # flag '--tb=no' is used to turn off tracebacks 